{
  "name": "@opensdkdev/opensdkdev",
  "private": true,
  "publishConfig": {
    "access": "restricted"
  },
  "scripts": {
    "build": "turbo run build",
    "build:force": "turbo run build --force",
    "check": "turbo run check",
    "provision": "node scripts/provision-sdks.mjs",
    "validate:packages": "node scripts/provision-sdks.mjs --validate-only"
  },
  "packageManager": "pnpm@10.31.0",
  "devDependencies": {
    "turbo": "2.8.20",
    "yaml": "2.8.3"
  }
}
